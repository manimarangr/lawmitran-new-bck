# leegality e-Stamp adapter (not implemented)

Placeholder for the **leegality** e-stamp provider. Only `mock` is implemented.
Implement `leegality-estamp.provider.ts` against `EStampProvider`
(`../../estamp-provider.interface.ts`), register it in `../../estamp.module.ts`,
and set `DOCS_ESTAMP_PROVIDER=leegality` (admin) or `ESTAMP_PROVIDER=leegality` (env).
See [docs/estamp-architecture.md](../../../../../../docs/estamp-architecture.md).
