# signdesk e-Stamp adapter (not implemented)

Placeholder for the **signdesk** e-stamp provider. Only `mock` is implemented.
Implement `signdesk-estamp.provider.ts` against `EStampProvider`
(`../../estamp-provider.interface.ts`), register it in `../../estamp.module.ts`,
and set `DOCS_ESTAMP_PROVIDER=signdesk` (admin) or `ESTAMP_PROVIDER=signdesk` (env).
See [docs/estamp-architecture.md](../../../../../../docs/estamp-architecture.md).
