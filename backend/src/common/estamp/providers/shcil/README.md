# shcil e-Stamp adapter (not implemented)

Placeholder for the **shcil** e-stamp provider. Only `mock` is implemented.
Implement `shcil-estamp.provider.ts` against `EStampProvider`
(`../../estamp-provider.interface.ts`), register it in `../../estamp.module.ts`,
and set `DOCS_ESTAMP_PROVIDER=shcil` (admin) or `ESTAMP_PROVIDER=shcil` (env).
See [docs/estamp-architecture.md](../../../../../../docs/estamp-architecture.md).
